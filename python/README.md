These are my solutions to the Matasano crypto challenges in Python.

Get set up with `virtualenv`:

```
virtualenv -p /path/to/python3 env;
. env/bin/activate
pip install -r requirements.txt
```
then run the challenges:

```
python challenges.py
```
